'use strict';

/**
 * attendance-policy service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::attendance-policy.attendance-policy');
