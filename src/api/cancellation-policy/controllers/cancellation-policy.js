'use strict';

/**
 *  cancellation-policy controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::cancellation-policy.cancellation-policy');
